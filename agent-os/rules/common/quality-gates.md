# Quality Gates

Run these checks before closing a task when relevant to the touched apps:

1. Lint must pass.
2. Typecheck must pass.
3. Build must pass for changed app targets.
4. Update the matching `<work-id>.log.md` with executed checks and outcomes.

Tests are optional unless explicitly requested for the task.
