This folder stores raw external references that inform implementation decisions.

- Clone inspiration repositories under `references/repos/<repo-name>/`.
- Keep raw notes, links, screenshots, and assets directly under `references/` or subfolders.
- Promote distilled patterns into `agent-os/examples/<library>/` once validated.
